# This file marks the 'common' package and ensures the directory is recognized as a Python module.
# Do not delete this file. It is intentionally left even if it appears empty.
# The 'common' package contains shared utilities and definitions used across multiple projects.
# Removing this may break workspace dependency resolution and other tooling that relies on it.
